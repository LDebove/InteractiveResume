import { ToolbarContent } from './toolbar-content';

describe('ToolbarContent', () => {
  it('should create an instance', () => {
    const directive = new ToolbarContent();
    expect(directive).toBeTruthy();
  });
});
